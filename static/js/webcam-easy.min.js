const canvasElement = document.getElementById('canvas');
if (!canvasElement) {
    throw "Canvas element is missing";
}

const dataUrl = canvasElement.toDataURL("image/png");
console.log(canvasElement.height, dataUrl);
